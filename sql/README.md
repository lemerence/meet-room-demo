# 初始化 SQL

T01 提供 `schema.sql`（会议室表）和 `data.sql`（3 间预置会议室）。
按此顺序在目标业务库手动执行，具体命令见根目录 README。

脚本可以重复执行：不会清表，不覆盖已存在 ID 的名称和容量。
`CREATE TABLE IF NOT EXISTS` 不负责修改既有表结构；已有同名表结构不一致时需先检查，不能当作迁移工具。
应用启动不自动执行初始化脚本。预约表在 T03 添加。
