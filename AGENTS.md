# 项目开发约束

## 项目范围与技术栈

会议室预约后端：会议室列表、创建预约、预约详情、按日期查询、取消预约。
不包含前端、登录权限、会议室管理、预约修改等功能，不擅自扩展需求。

- Java 21、Spring Boot 3.3.5、MyBatis-Plus 3.5.9（Boot 3 Starter）。
- MySQL 8.4.11（InnoDB）、Maven 3.9.16。
- Spring MVC、Jakarta Validation、Spring 事务。
- Spring Boot Test、JUnit 5、MockMvc，按需使用已有 Mockito。
- 不引入数据库迁移、测试容器管理、Redis、消息队列等额外工具。

## 项目结构与架构

单 Maven 工程，根包 `com.example.meetroom`：

- `interfaces`：Controller、请求响应 DTO、异常映射、请求时间采集。
- `application`：用例编排、事务边界。
- `domain`：会议室与预约聚合、时间段、业务规则、仓储接口。
- `infrastructure`：数据库实体、Mapper、仓储实现、配置。
- `src/test`：单元、集成及端到端测试；`sql`：后续业务初始化 SQL。
- `README.md`：启动、配置及验证说明。
- 根目录 `API.md`：已实现且测试通过的接口文档。

接口层调用应用层，应用层依赖领域层，基础设施实现仓储接口。
Controller 不直接调用 Mapper；领域对象不依赖持久化框架。
会议室与预约是独立聚合，预约通过会议室 ID 关联会议室。
按实际需要增加子目录，不创建无用途的占位类。

## 核心业务与时间规则

- 会议室必须存在；员工 ID 必填，不核验身份。
- 主题去除首尾空白后非空，最长 200 字符；人数为正整数且不超过容量。
- 开始早于结束，单次不超过 4 小时；不允许跨天，结束于次日零点除外。
- 同一会议室未取消预约不得重叠，首尾衔接允许。
- 取消保留记录并释放时段；已取消优先返回“预约已经取消无需重复取消”，不改写取消时间。
- 日期查询只返回未取消记录（包括已结束），按开始时间、ID 升序；详情包含取消记录。
- 统一北京时间。预约 `yyyy-MM-dd HH:mm`，审计 `yyyy-MM-dd HH:mm:ss`。
- 合法预约输入携带秒或更低精度时直接截去，不四舍五入；归一化后校验和存储。
- 时间区间为左闭右开，结束时刻不占用时间。
- 服务端入口记录一次请求到达时间，不信任客户端时间、不截断到分钟、不因等锁更新。
- 创建时开始时间须晚于请求到达时间；取消请求须在开始之前到达。
- 审计时间记录实际操作时间，不复用请求到达时间。

## 事务与编码规范

- 创建、取消事务放在应用服务，使用 READ COMMITTED。
- 统一锁顺序：会议室 → 预约；锁后读取最新状态并检查冲突。
- 锁、检查、写入处于同一事务，异常回滚；不吞异常后返回成功。
- 请求到达时间只决定时间资格，不代表并发优先级。
- 使用构造器注入，职责清晰，避免无关抽象和重构。
- DTO、领域对象、数据库实体分离；SQL 参数化，不拼接用户输入。
- 取消使用业务状态，不使用物理删除或逻辑删除。
- 返回明确业务错误，不暴露 SQL、堆栈或凭据。
- 类需要类注释，标识这个类的用途，方法需要方法注释标识这个方法用途，关键代码、负责逻辑也需要增加注释介绍说明代码的逻辑,成员变量也需要有注释，表明这个变量含义。

## 运行方式

- MySQL 容器 `meet-room-mysql`，业务库/账号 `meet_room`，本机端口 3306。
- 测试库/账号 `meet_room_test`；测试账号不得访问业务库。
- `docker start meet-room-mysql` 启动数据库。
- Maven 镜像 `meet-room-maven:3.9.16-java21`，缓存卷 `meet-room-maven-cache`。
- Maven 使用 `mvn clean package` 构建、`mvn spring-boot:run` 启动。
- 本机无 Maven 时使用 README 中的 Docker 命令。
- `.env.local`、`.env.test` 仅用于本地凭据，已忽略；Spring 不自动读取它们，Docker 通过 `--env-file` 传入。
- T01 会议室表及预置数据位于 `sql/schema.sql`、`sql/data.sql`，手动初始化；启动时不得清表或覆盖数据。

## 安全与依赖约束

- 不随意新增、升级依赖或安装工具，须说明用途并取得确认。
- 密码不硬编码、不提交、不打印；仅使用外部本地配置。
- 测试只能操作隔离测试库，禁止清理业务库。
- 不擅自删除数据、数据卷、重置环境、覆盖用户修改或扩大网络暴露范围。
- 未获授权不提交、推送或发布代码。

## 开发与验收

- 按确认的任务逐项实施；只要求分析或输出时，不修改文件、不安装依赖、不编码。
- 业务规则先写测试，再实现；时间边界使用可控时钟。
- 从首个业务功能开始，逐项执行真实 HTTP → 应用 → MySQL 端到端验证，MockMvc 不能替代。
- 并发与事务使用真实 MySQL 验证；验证重启后预约和取消状态保留。
- 完成时报告改动、实际验证结果和未验证事项；未执行验证不得宣称通过。
- 每个接口实现并通过相关测试（包括端到端验证）后，必须同步更新根目录 `API.md`，包含请求、响应、错误码、业务边界和验证结果；未通过测试不得标为已交付。
